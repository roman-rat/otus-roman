user nginx;
worker_processes auto;
pid /run/nginx.pid;

events {
    worker_connections 4096;
}

http {
    server {
        listen {{ nginx_port }};

        location / {
            root /var/www/html;
            index index.html index.htm;
        }
    }
}